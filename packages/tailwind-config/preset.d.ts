import type { Config } from 'tailwindcss';

declare const preset: Partial<Config>;
export default preset;

/** CSS variables to paste into the storefront's globals.css. */
export declare const celvaCssVariables: string;
