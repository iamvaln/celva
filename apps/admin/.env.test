# Test-only env. Vitest runs with mode=test, so vite.config's loadEnv picks
# this up automatically — no real API is contacted (the dataProvider/fetch is
# mocked in tests). Values are placeholders that satisfy the required-var check.
VITE_API_URL=http://localhost:3001
VITE_STOREFRONT_URL=http://localhost:3000
