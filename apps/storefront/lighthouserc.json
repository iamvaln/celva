{
  "$comment": "Lighthouse CI config. Run `npm run lighthouse` from apps/storefront (builds, boots `next start` on :4317 — a dedicated port so it can't collide with a dev server on :3000 — audits, asserts). For real catalogue/journal numbers the API must be reachable (API_INTERNAL_URL); when it isn't, those routes degrade to their empty state and perf is optimistic.",
  "ci": {
    "collect": {
      "startServerCommand": "npx next start --port 4317",
      "startServerReadyPattern": "Ready in",
      "url": [
        "http://localhost:4317/fr",
        "http://localhost:4317/en",
        "http://localhost:4317/fr/boutique",
        "http://localhost:4317/fr/journal"
      ],
      "numberOfRuns": 2,
      "settings": { "preset": "desktop" }
    },
    "assert": {
      "assertions": {
        "categories:performance": ["warn", { "minScore": 0.8 }],
        "categories:accessibility": ["error", { "minScore": 0.9 }],
        "categories:best-practices": ["warn", { "minScore": 0.9 }],
        "categories:seo": ["error", { "minScore": 0.9 }]
      }
    },
    "upload": { "target": "temporary-public-storage" }
  }
}
